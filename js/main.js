$( document ).ready(function() {

    // Fade out PHP generated alerts
    $(".alert").fadeTo(3500, 600).slideUp(400, function () {
        $(".alert").slideUp(600);
        $(".alert").remove();
    });

    // Load the Buckets

    // Construct bucket objects

    // Attach Events to Bucket links

    // Inject bucket objects

    // Set active Bucket

    // Fetch all categories and tasks in current buckets

    // Construct category containers

    // Attach Event to category buttons

    // Construct Task objects

    // Attach events to task objects

    // Inject Task Objects into Category containers

    // Inject Category Containers into body


});
